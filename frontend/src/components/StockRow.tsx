// See ARCHITECTURE.md §10.1 — ticker, reason, price, rvol, change%
// TODO: See ARCHITECTURE.md §10.1 — render stock data row
// Rule 6 (WINDSURF.md): all numbers must use toFixed(2) — no raw floats

export default function StockRow(): JSX.Element {
  // TODO: See ARCHITECTURE.md §10.1 — props: StockObject per §9.3 schema
  // Rule 6: all displayed numbers must call .toFixed(2)
  return <></>;
}
