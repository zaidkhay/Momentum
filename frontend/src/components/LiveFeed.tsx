// See ARCHITECTURE.md §10.1 — ranked stock list, re-renders every 1s
// TODO: See ARCHITECTURE.md §10.1 — use useSectorFeed hook,
// render StockRow for each stock, apply flash-up/flash-down on price change

export default function LiveFeed(): JSX.Element {
  // TODO: See ARCHITECTURE.md §10.1 + §10.2 — 1s polling via useSectorFeed
  return <></>;
}
