// See ARCHITECTURE.md §10.3 — Hopeful tab layout
// TODO: See ARCHITECTURE.md §10.3 — leaders (isHopeful:true, isSympathy:false)
// above divider, sympathy plays (isSympathy:true) below, grouped by parent

export default function HopefulFeed(): JSX.Element {
  // TODO: See ARCHITECTURE.md §10.3 + §10.2 — 1s polling via useHopeful
  return <></>;
}
